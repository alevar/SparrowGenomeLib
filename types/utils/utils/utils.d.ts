import { Interval } from '../../types';
export declare function adjustIntervals(intervals: Interval[], start: number, end: number, separator: number): Interval[];
export declare function computeMidpoint(a: number, b: number): number;
